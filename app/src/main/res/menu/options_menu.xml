<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!-- New Note -->
    <item
        android:id="@+id/action_bar_new"
        android:icon="@drawable/baseline_note_add_black_24"
        android:title="@string/new_note"
        android:contentDescription="@string/navigate_to_the_new_note_activity"
        app:showAsAction="ifRoom" />

    <!-- List -->
    <item
        android:id="@+id/action_bar_list"
        android:icon="@drawable/baseline_view_list_black_24"
        android:title="@string/note_list"
        android:contentDescription="@string/navigate_to_the_notes_list"
        app:showAsAction="ifRoom" />

    <!-- Settings -->
    <item
        android:id="@+id/action_bar_settings"
        android:icon="@drawable/baseline_settings_black_24"
        android:title="@string/settings"
        android:contentDescription="@string/navigate_to_the_settings_menu"
        app:showAsAction="never" />

</menu>
