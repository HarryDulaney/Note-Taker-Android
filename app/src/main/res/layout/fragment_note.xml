<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/colorPrimaryDark"
    tools:context="frags.">

    <!-- Note Title-->
    <TextView
        android:id="@+id/textNoteTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/smallMargin"
        android:layout_marginLeft="@dimen/smallMargin"
        android:layout_marginTop="@dimen/smallMargin"
        android:text="@string/title"
        android:textAppearance="?attr/textAppearanceListItem"
        android:textColor="@color/hgdivDarkItems"
        android:textSize="20sp"
        android:textStyle="bold" />

    <!-- Delete Button-->
    <ImageButton
        android:id="@+id/buttonDeleteNote"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@id/textNoteTitle"
        android:layout_alignRight="@id/textNoteTitle"
        android:onClick="handleDeleteNote"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_centerInParent="true"
        android:layout_marginStart="@dimen/smallMargin"
        android:layout_marginLeft="@dimen/smallMargin"
        android:layout_marginTop="@dimen/smallMargin"
        android:layout_marginEnd="@dimen/smallMargin"
        android:layout_marginRight="@dimen/smallMargin"
        android:layout_marginBottom="@dimen/smallMargin"
        android:adjustViewBounds="true"
        android:background="#00FFFFFF"
        android:contentDescription="@string/delete_button_for_this_note"
        android:focusable="false"
        android:padding="@dimen/extraSmMargin"
        android:scaleType="center"
        android:src="@android:drawable/ic_delete"
        android:visibility="invisible" />

    <!-- Date Created -->
    <TextView
        android:id="@+id/dateCreatedText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/textNoteTitle"
        android:layout_marginStart="@dimen/smallMargin"
        android:layout_marginLeft="@dimen/smallMargin"
        android:layout_marginTop="@dimen/smallMargin"
        android:padding="@dimen/fab_margin"
        android:text="@string/d_a_t_e"
        android:textAllCaps="true"
        android:textAppearance="?attr/textAppearanceListItem"
        android:textColor="@color/hgdivDarkItems" />

</RelativeLayout>
